Flask==3.0.3
Werkzeug==3.0.3
# Note: torch is installed separately in Dockerfile using CPU wheel for smaller image size
# For local install: pip install torch==2.3.0 --index-url https://download.pytorch.org/whl/cpu
torch==2.3.0
transformers==4.40.1
Pillow==10.3.0
groq==0.9.0
gunicorn==22.0.0
